{
  "name": "logan-worker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute logan-db --local --file=./schema.sql",
    "db:init:remote": "wrangler d1 execute logan-db --remote --file=./schema.sql",
    "cf-login": "wrangler login"
  },
  "dependencies": {
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "wrangler": "^3.90.0",
    "typescript": "^5.6.0"
  }
}
